import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-sponsor',
  standalone: true,
  imports: [RouterModule],
  templateUrl: './sponsor.html',
  styleUrl: './sponsor.scss'
})
export class Sponsor {

}
